#version 450
#extension GL_EXT_mesh_shader : require

#include "node_mesh_constants.h"

void main()
{
	uint xGroups = SCREEN_X / (QUAD_DIMENSION_COUNT * SCALE);
	uint yGroups = SCREEN_Y / (QUAD_DIMENSION_COUNT * SCALE);
	EmitMeshTasksEXT(xGroups, yGroups, 1);
}