#version 450
#extension GL_EXT_mesh_shader : require

void main()
{
	uint xPixels = 1920;
	uint yPixels = 1080;
	uint xGroups = xPixels / 8;
	uint yGroups = yPixels / 8;
	uint totalGroups = xGroups * yGroups;

	EmitMeshTasksEXT(totalGroups, 1, 1);
}